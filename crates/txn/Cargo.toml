[package]
name = "txn"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[features]
# Exposes `LockManager::held_lock_count`, for a caller outside this crate's
# own `#[cfg(test)]` binary (`engine`'s integration tests) to assert every
# lock a transaction held was actually released.
test-util = []

[dependencies]
common = { workspace = true }
storage = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
metrics = { workspace = true }

[dev-dependencies]
test-support = { workspace = true }
tempfile = { workspace = true }
