name: CI

on:
  pull_request:
  push:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Check docs convention
        run: bash scripts/check_docs.sh

      - name: Build (no dev-dependencies, no test-util)
        run: cargo build --workspace

      - name: Test
        run: cargo test --workspace

      - name: Test (buffer pool concurrency, isolated from other tests' cores)
        run: cargo test -p storage --test buffer_pool_concurrency -- --test-threads=1
